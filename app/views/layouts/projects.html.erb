<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />  
  <title>Specright: Specifications</title>    
    <%= javascript_include_tag 'application-main' %>
    <%= stylesheet_link_tag  'application-main', media: 'all' %>   
    <%= javascript_tag "var AUTH_TOKEN = #{form_authenticity_token.inspect};" if protect_against_forgery? %>
	<%= csrf_meta_tags %>
</head>

<body>

<div class="app_page_container">

    <header class="app_page_header">
        <figure class="app_logo"></figure>	
	    
              <nav class="lrg_menu">
            	    <!-- menu header-->
                    <div class='menu_container'>
                    <ul>
                       <li class='has-sub'><%= link_to "PROJECT", edit_project_path(@project.id)%></a>
                          <ul>
                             <li><%= link_to "Select Project", edit_project_path(@project.id)%></a></li>
                             <li><%= link_to "Create Project", edit_project_path(@project.id)%></a></li>
                             <li><%= link_to "Edit Project Details", edit_project_path(@project.id)%></a></li>
                             <li><%= link_to "Project Authorisation", edit_project_path(@project.id)%></a></li>
                          </ul>
                       </li>
                       <li><%= link_to "SPECIFICATION", edit_project_path(@project.id)%></a>
                       <li><%= link_to "REVISIONS", edit_project_path(@project.id)%></a>
                       <li class='has-sub'><%= link_to "PUBLISH", edit_project_path(@project.id)%></a>
                          <ul>
                             <li><%= link_to "Download/Publish PDF", edit_project_path(@project.id)%></a></li>
                             <li><%= link_to "PDF Settings", edit_project_path(@project.id)%></a></li>
                             <li><%= link_to "Download Keynote File", edit_project_path(@project.id)%></a></li>
                          </ul>
                       </li>
                       <li class='has-sub right'><%= link_to current_user.name, edit_project_path(@project.id)%></a>
                         <ul>
                             <li><%= link_to "Download/Publish PDF", edit_project_path(@project.id)%></a></li>
                             <li><%= link_to "PDF Settings", edit_project_path(@project.id)%></a></li>
                             <li><%= link_to "Download Keynote File", edit_project_path(@project.id)%></a></li>
                          </ul>      
                       </li>                                         
                    </ul>
                    </div>
              </nav>              

            <nav class="app_logout"><%= link_to "logout", log_out_path %></nav>     
    </header>	

<%= yield %>

</body>

</html>