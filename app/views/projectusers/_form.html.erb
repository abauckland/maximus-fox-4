<%= form_for(@projectuser) do |f| %>
  <% if @projectuser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projectuser.errors.count, "error") %> prohibited this projectuser from being saved:</h2>

      <ul>
      <% @projectuser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.number_field :project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
