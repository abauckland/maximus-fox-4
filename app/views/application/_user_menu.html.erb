<nav class="user_menu"><%= current_user.name %></nav>
<nav>
    <ul class='right_menu'>
        <li><%= link_to "Edit My Details", edit_user_registration_path(current_user.id)%></li>
    <% if current_user.role == 'owner'%>
        <li><%= link_to "Company Details", edit_company_path(current_user.company_id)%></li>
        <li><%= link_to "Manage Users", users_path %></li> 
    <% end %> 
        <li><%= link_to "logout", destroy_user_session_path, method: :delete %></li>
    </ul>
</nav>